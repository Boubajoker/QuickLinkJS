{
    "configurations": [
        {
            "name": "Launch QuickLinkJS WebDoc's on a localhost",
            "request": "launch",
            "type": "pwa-chrome",
            "url": "http://127.0.0.1:3520/docs/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}